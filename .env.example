# ================== Environment Variables Example File ==================
# Usage:
# 1. Copy this file as `.env.local`
# 2. Update the values according to your local / deployment environment
# 3. Make sure `.env.local` is added to .gitignore and NEVER committed to version control

# ========== GitHub Configuration ==========
# [Sensitive] GitHub access token, used to access the GitHub API (do NOT expose or commit this value)
# Common usage:
# - Generate a Classic Token or Fine-grained Token in your GitHub settings
# - Only enable the minimal scopes required by this project (e.g. repo, read:org)
GITHUB_ACCESS_TOKEN="YOUR_GITHUB_ACCESS_TOKEN_HERE"

# ========== Better Auth Configuration ==========
# [Sensitive] Better Auth secret used to sign/encrypt session cookies (do NOT expose or commit this value)
# Purpose:
# - Used by the server to sign session data and prevent cookie tampering
# - Changing this value invalidates existing sessions (users will need to sign in again)
# How to generate:
# - Run: openssl rand -base64 32
# - Or use any secure random string generator
AUTH_SECRET="YOUR_AUTH_SECRET_HERE"

# Public base URL of your AUTH service (Better Auth uses it to generate absolute callback/redirect URLs)
AUTH_URL="http://localhost:8000"

# [Sensitive] GitHub OAuth App Client ID (do NOT expose or commit this value)
# How to obtain:
# - Go to GitHub Settings > Developer settings > OAuth Apps
# - Create a new OAuth App or use an existing one
# - Set Authorization callback URL to: https://<your-domain>/api/auth/callback/github
# - Copy the Client ID from the app settings page
AUTH_GITHUB_ID="YOUR_GITHUB_OAUTH_CLIENT_ID_HERE"

# [Sensitive] GitHub OAuth App Client Secret (do NOT expose or commit this value)
# How to obtain:
# - In the same OAuth App settings page, generate a new client secret
# - Copy the secret immediately (it will only be shown once)
AUTH_GITHUB_SECRET="YOUR_GITHUB_OAUTH_CLIENT_SECRET_HERE"

# ========== AI Service Base Configuration ==========
# Base URL of the AI HTTP endpoint, must be compatible with the OpenAI API protocol
# Common examples:
# - OpenAI official: https://api.openai.com/v1
# - DeepSeek:        https://api.deepseek.com/v1
# - Tongyi Qianwen (proxy example): https://dashscope.aliyuncs.com/compatible-mode/v1
AI_BASE_URL=""

# [Sensitive] AI service API key, used for authentication when calling the AI API (do NOT expose or commit this value)
# How to obtain:
# - Log in to the corresponding AI service console and create a key in the API Key / Access Key section
# - Restrict permissions and quota based on actual needs to reduce potential abuse
AI_API_KEY="YOUR_AI_API_KEY_HERE"

# ========== AI Model Configuration ==========
# Default model name to use; it must exactly match a model supported by the selected AI service
# Common examples:
# - OpenAI:   gpt-4o-mini, gpt-4.1-mini, gpt-4.1
# - DeepSeek: deepseek-chat, deepseek-reasoner
# - Other compatible providers: please refer to their official model list documentation
AI_MODEL=""

# ========== Umami Analytics Configuration ==========
# Enable or disable Umami analytics tracking
# - Set to "true" to enable tracking in production
# - Set to "false" or leave empty to disable tracking
# - Note: Tracking is automatically disabled in development mode
NEXT_PUBLIC_UMAMI_ENABLED="true"

# Umami script URL (usually from Umami Cloud or self-hosted instance)
# - Default: https://cloud.umami.is/script.js
NEXT_PUBLIC_UMAMI_SCRIPT_URL="https://cloud.umami.is/script.js"

# [Public] Umami website ID for tracking
# - Obtain this from your Umami dashboard when you add a new website
# - Different environments (dev/staging/prod) can use different website IDs
NEXT_PUBLIC_UMAMI_WEBSITE_ID=""

# (Optional) Comma-separated list of domains where tracking should be active
# - Leave empty to track on all domains
# - Example: "green-wall.leoku.dev,www.green-wall.leoku.dev"
NEXT_PUBLIC_UMAMI_DOMAINS=""
